import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary:{
            main: "#E5AFF5"
        },
        secondary:{
            main: "#4E7448"
        }
    }
})

export default theme;